# 📊 Professional Friend — Project Activities & Technical Matrix

This tracking matrix details the **Action Items, Sub Tasks, Status, Notes, and Tools/Languages/Frameworks** for the **Professional Friend** project, matching the dedicated sheet created in `Daily Activities 12.54.25 PM.xlsx`.

---

| Action Items | Sub Tasks | Status | Note | Tools / Languages / Frameworks |
|---|---|:---:|---|---|
| **1. 3D Avatar & Virtual Environment Rendering** | • Load Ready Player Me avatar (`aura.glb`) with 52 Apple ARKit blendshapes<br>• Retarget and loop Mixamo FBX animations (`Greeting`, `Talking`, `Happy`, `Clapping`, `Dancing`, `Sitting Interview`)<br>• Build interactive 3D Office (`Cyber Chair`, Desk, Laptop, Bookshelf) and Nature environments<br>• Configure PBR lighting, PMREM RoomEnvironment, and Three.js OrbitControls | `Completed` | Stripped `mixamorig` bone prefixes for seamless retargeting. Fixed avatar orientation to face forward (`rotation.y = 0`). | **Three.js**, **WebGL 2.0**, **GLTFLoader**, **FBXLoader**, **React 19**, **TypeScript** |
| **2. Real-Time Biometric Face Tracking & Emotion Detection** | • Client-side Google MediaPipe FaceLandmarker running at 60 FPS<br>• Extract 478 3D facial landmarks and live gaze vector<br>• Classify candidate emotional states (*Neutral*, *Happy*, *Surprised*, *Sad*, *Focused*)<br>• Synchronize candidate visual feedback with interviewer prompt context | `Completed` | Runs entirely client-side via WASM with zero server latency and 100% user privacy. | **`@mediapipe/tasks-vision`**, **WebRTC MediaDevices**, **HTML5 Canvas**, **TypeScript** |
| **3. Real-Time Environment Object Detection** | • Real-time webcam object detection via TensorFlow.js COCO-SSD<br>• Identify physical objects (*laptop*, *cell phone*, *book*, *cup*, *chair*, *person*)<br>• Inject detected objects into LLM prompt for environmental grounding | `Completed` | WebGL hardware acceleration with dynamic confidence threshold filtering and bounding boxes. | **`@tensorflow/tfjs`**, **`@tensorflow-models/coco-ssd`**, **WebGL backend** |
| **4. Real-Time Technical Interview & Dynamic Question Generation** | • Dynamic 15-question interview bank generation for top tech companies (*Meta*, *Google*, *Apple*, *OpenAI*)<br>• Live Google Search Grounding with Gemini 2.5 Flash for verified real-time citations<br>• Fallback question compilation via Mistral Large<br>• Contextual candidate assessment based on emotion, role, and turn history | `Completed` | Questions categorized into Coding, System Design, Behavioral (STAR), and Domain-specific technical questions. | **`@google/genai` (Gemini 2.5 Flash)**, **Google Search Grounding**, **Mistral Large API**, **TypeScript** |
| **5. Multi-Band Conversational Lip-Sync & Formant Calibration** | • Real-time Web Audio FFT frequency formant analyser (200Hz–8000Hz)<br>• Dynamic ARKit blendshape modulation (`jawOpen`, `mouthFunnel`, `mouthSmile`, `mouthPucker`)<br>• Calibrate `jawOpen` bound to 0.05–0.35 to prevent unhinged/wide mouth opening<br>• Parallel NVIDIA Audio2Face blendshape streaming integration | `Completed` | Natural human conversational bounds applied across all vowel and consonant formants. | **Web Audio API (`AudioContext`, `AnalyserNode`)**, **Apple ARKit 52 Blendshapes**, **NVIDIA Audio2Face** |
| **6. Speech Interruption & Overlapping Speech Cancellation** | • Implement `stopSpeaking()` to immediately halt active Web Audio nodes<br>• Reset speech queue (`speakQueueRef`) and discard pending in-flight audio<br>• Advance speech turn ID (`currentSpeechTurnRef`) to invalidate stale responses<br>• Auto-interrupt speech on mic click when candidate starts speaking | `Completed` | Completely eliminates double-voice overlap and response collision when new messages arrive. | **Web Audio API**, **React Refs**, **JavaScript Promises**, **TypeScript** |
| **7. Standalone Port 3000 Architecture & Direct Vite Cloud Proxies** | • Eliminate port 3001 Express server dependency for 100% standalone frontend execution<br>• Configure direct Vite cloud proxies (`/api/mistral`, `/api/deepgram`, `/api/groq`, `/api/cartesia`)<br>• Setup single-entry environment variables (`envPrefix` in `vite.config.ts`)<br>• Remove unused packages (`better-sqlite3`, `fbx2gltf`, `express`, `http-proxy-middleware`) | `Completed` | Simplified development workflow: single command execution (`npm run dev`) on port 3000. | **Vite 6**, **Tailwind CSS v4**, **TypeScript**, **React 19** |
